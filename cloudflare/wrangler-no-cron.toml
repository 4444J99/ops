# Retired deployment configuration retained for custody. It cannot run products.
# The canonical production release uses wrangler.toml through scripts/release.py.
name = "ops-scheduler"
main = "src/index.ts"
compatibility_date = "2026-09-17"
compatibility_flags = ["nodejs_compat"]
services = []
[vars]
OPS_CONTROLLER_ENV = "disabled"
[triggers]
crons = []
